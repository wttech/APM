plugins {
  id 'com.cognifide.aem.package'
}

apply from: "common.gradle"

defaultTasks = [':aem']

task aem(dependsOn: [':aemDeploy'])

aemCompose {
  includeProject ':bundle'
  includeProject ':content'
}